CineBar 0.9.0-test.1 · Build 113

- 修复：卡片列数滑块可自由拖动。根因是全局「自由拖动」手势抢占了滑块拖动手势（所以之前只有锁定窗口时滑块才正常）；已移除该手势，改用原生背景拖动，滑块不再被抢占。
- 新增：主窗口顶部右键可弹出「锁定窗口 / 取消锁定」菜单，控制面板是否可拖动。
- 调整：菜单栏右键「AI 搜索」改为「AI 问电影」。

本版本为测试版，未经过 Apple 公证。更新包仍通过 CineBar 内置签名校验。
