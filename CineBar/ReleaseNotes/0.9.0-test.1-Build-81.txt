CineBar 0.9.0-test.1 · Build 81

- 新增：CineAI 对话记忆——历史对话会保存，重进聊天界面甚至重启后仍在。
- 调整：主页顶部默认显示 CineAI 入口框（点击进入聊天）；顶栏改为 搜索(放大镜) / 分类 / 设置(最右)，隐藏了普通搜索栏（点放大镜切出）；去掉了"本周热门"按钮。
- 优化：CineAI「今晚看什么」只推荐你本地磁力库里已有资源的影片，不再推空有评分但无法下载/播放的片。

本版本为测试版，未经过 Apple 公证。更新包仍通过 CineBar 内置签名校验。
