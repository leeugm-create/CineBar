CineBar 0.9.0-test.1 · Build 104

- 修复：CineAI 多轮长对话后报「AI 返回了无法解析的内容」。根因是历史消息全量发送导致请求体过大，代理返回 413「input too large」；现已裁剪历史（最近 3 轮、限制总长度），对话再长也不会触发。
- 新增：设置「错误日志」栏——可查看本地日志、上传错误日志、或通过邮箱反馈问题。

本版本为测试版，未经过 Apple 公证。更新包仍通过 CineBar 内置签名校验。
