CineBar 0.9.0-test.1 · Build 89

- 新增：菜单栏右键菜单内嵌 AI 搜索框（可输入文字、带提示、回车即搜）。
- 改进：CineAI 推荐改为按主题出片单（励志/爱情/悬疑等），不再只回退到数量极少的默认候选。
- 新增：CineAI 回答里的片名自动配海报小卡（最多 5 部，点击进详情）。

本版本为测试版，未经过 Apple 公证。更新包仍通过 CineBar 内置签名校验。
