CineBar 0.8.3-test.12 · Build 28

- 自动匹配建议和手动搜索现在共用可取消请求；关闭、确认或新请求会阻止迟到结果覆盖当前条目。
- 同名候选现在优先选择与文件名年份一致的版本，再沿用评分、标题和 ID 的确定性排序。
- TMDB 缺少上映或首播日期时，只要 ID 和标题有效，确认后的电影或电视剧仍可打开现有完整详情。
- 保留 Build 27 与更早版本的不可变归档和 appcast 历史条目。

本版本为测试版。应用本体仅使用 ad-hoc（临时）代码签名，不是 Apple Developer ID
签名，也未经 Apple 公证；Sparkle EdDSA 仅用于验证更新归档，不能替代应用代码签名
或 Apple 公证。
